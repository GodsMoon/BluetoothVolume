<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/primary</item>
        <!--   theme UI controls like checkboxes and text fields-->
        <item name="android:colorAccent">@color/accent</item>

        <item name="android:navigationBarColor">@color/primary_dark</item>

        <item name="android:textColorPrimary">@color/title_text</item>
    </style>

    <style name="SeekBar" parent="android:Widget.Material.SeekBar">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="SeekBarGreen" parent="SeekBar">
        <item name="android:colorAccent">@color/green_seek_bar</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="SeekBarBlue" parent="SeekBar">
        <item name="android:colorAccent">@color/accent</item>
    </style>

</resources>
